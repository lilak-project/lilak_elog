# TODO / Deferred

Things intentionally parked for later.

## Settings
- **Density & Size controls** — removed from the system drawer (2026-06-14) to keep
  it compact (account / theme / language only). The `DensityContext` / `SizeContext`
  and their tokens still exist; re-expose these controls (likely under a Settings-tab
  "Appearance" section) when we revisit display density.
